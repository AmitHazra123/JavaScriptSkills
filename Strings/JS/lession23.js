var str1 = "John";
var str2 = new String("John");

if(str1 == str2)
    document.write("Two Strings are same.");
else
    document.write("Two Strings are not same.");

if(str1 === str2)
    document.write("Two Strings types are same.");
else
    document.write("Two Strings types are not same.");